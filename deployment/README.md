# TimeLord Deployment

Packaging and deployment assets:

- `docker/` — controller Dockerfile and Compose stack
- `windows/` — agent service installer / registration scripts
- `systemd/` — controller systemd unit for bare-metal Linux hosts

Not yet populated.
