# TimeLord Protocol

Versioned wire protocol: JSON Schemas for signed messages (policies, leases, commands) under
`schemas/`, with worked examples under `examples/`. Not yet populated — the agent currently
only tracks local usage sessions and doesn't yet talk to a controller.
