# TimeLord Controller

Central controller service (Java 21 / Spring Boot). Not yet implemented — placeholder
directory reserving the layout described in the project design brief
(`controller/src/main/java`, `controller/src/main/resources`, `controller/src/test`).
